/**
 * js doc ...
 */
import cheerio from 'cheerio'
import getContent from './../getContent'

const url = 'http://stocks.finance.yahoo.co.jp/stocks/history/?code=EURJPY=X'

export default async function(){
  return await {
    a: 'test',
  }
}
